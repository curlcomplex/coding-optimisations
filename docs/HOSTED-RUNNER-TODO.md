# Deferred: Hosted GitHub Runner

Do not implement until the self-hosted Mac path is working and measured.

Future work should determine whether ChatGPT-subscription authentication can be used safely and reliably on an ephemeral hosted runner without an OpenAI API key, how refresh credentials would be protected, and whether its usage accounting is comparable to the self-hosted baseline.
